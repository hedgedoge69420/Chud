# Optional: enables live SiteSafe AI Copilot responses. Without it, the copilot
# runs in evidence-safe demo mode with streamed deterministic tools.
OPENAI_API_KEY=
# Optional model override. Defaults to gpt-5.
OPENAI_MODEL=
# Public agent ID from the ElevenLabs Agents dashboard.
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=
# Reserved for future server-side caching.
KV_REST_API_URL=
KV_REST_API_TOKEN=
