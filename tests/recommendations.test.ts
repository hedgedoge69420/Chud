import {describe,it,expect} from 'vitest';import {demoReports} from '@/lib/demo-data';import {recommendations} from '@/lib/recommendations';describe('recommendations',()=>{it('triggers bushfire, intended-use heat and flood-gap actions deterministically',()=>{expect(recommendations(demoReports[0]).map(x=>x.id)).toEqual(['bal','vegetation','home-heat','flood-advice']);expect(recommendations(demoReports[1]).map(x=>x.id)).toEqual(['farm-heat','flood-advice'])});it('never recasts flood not assessed as low risk',()=>{expect(demoReports[0].findings.find(x=>x.id==='flood')?.finding).toContain('not assessed')})});
