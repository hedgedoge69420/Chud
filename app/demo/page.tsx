'use client';import {useState} from 'react';import {demoReports} from '@/lib/demo-data';import {ReportView} from '@/components/report-view';
export default function Demo(){const [id,setId]=useState(demoReports[0].id),report=demoReports.find(r=>r.id===id)!;return <main><div className="page-head"><span className="tag warn">Saved demonstration data</span><h1>A complete journey,<br/>available offline.</h1><p className="lead">Explore illustrative Victorian property reports without relying on external services.</p></div><div className="container"><div className="actions" style={{marginTop:0,marginBottom:36}}>{demoReports.map(r=><button className={'button '+(r.id===id?'':'secondary')} key={r.id} onClick={()=>setId(r.id)}>{r.location.locality}</button>)}</div><ReportView report={report}/></div></main>}
