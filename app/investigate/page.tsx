import {Investigator} from '@/components/investigator';import {pageMetadata} from '@/lib/page-metadata';
export const metadata=pageMetadata('Investigate a property | SiteSafe','Locate a Victorian address, screen live Vicmap and Open-Meteo evidence, and act on transparent next steps.');
export default async function Investigate({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;return <main><div className="page-head regrow-head"><div className="eyebrow">Live evidence screen</div><h1>Climate intelligence for Victorian land.</h1><p className="lead">Locate the property. Screen traceable evidence. Act on clear next steps.</p></div><div className="investigate-shell"><Investigator initialQuery={q}/></div></main>}
