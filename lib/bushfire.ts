import 'server-only';import {z} from 'zod';import {fetchJson} from './fetch-with-policy';
const Result=z.object({features:z.array(z.object({attributes:z.record(z.string(),z.unknown())}))});
export async function bushfireIntersection(lon:number,lat:number){const url=new URL('https://services-ap1.arcgis.com/P744lA0wf4LlBZ84/ArcGIS/rest/services/Vicmap_Planning/FeatureServer/9/query');url.search=new URLSearchParams({f:'json',where:'1=1',geometry:`${lon},${lat}`,geometryType:'esriGeometryPoint',inSR:'4326',spatialRel:'esriSpatialRelIntersects',outFields:'OBJECTID,lga_code,lga_name,plan_number,gazettal_date,bpa_area_ha',returnGeometry:'false',resultRecordCount:'1'}).toString();const r=Result.parse(await fetchJson(url));return {intersects:r.features.length>0,fields:r.features[0]?.attributes??{}};}
