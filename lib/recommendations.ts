import {catalogue} from '../data/adaptation-catalogue';
import type {SiteReport} from './schemas';
export function recommendations(report:SiteReport){const hotter=report.projections.some(p=>p.indicator==='days_over_35'&&p.medianChange>0);return catalogue.filter(a=>a.id==='flood-advice'||(a.category==='bushfire'&&report.bushfire===true)||(hotter&&a.id===(report.context.use==='farm'?'farm-heat':report.context.use==='warehouse/commercial'?'warehouse-heat':'home-heat'))).map(a=>({...a,findingId:a.category==='bushfire'?'bushfire':a.category==='heat'?'heat':'flood',reason:a.category==='bushfire'?'A mapped bushfire intersection was returned.':a.category==='heat'?`The ensemble median indicates more days above 35°C; intended use: ${report.context.use}.`:'Flood evidence has not been assessed.'}));}
export function decisionSummary(r:SiteReport){return r.bushfire===true?'Material mapped exposure identified — specialist advice recommended':'Additional investigation recommended';}
