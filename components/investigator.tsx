'use client';
import {useState} from 'react';import useSWR from 'swr';import type {DecisionContext,SiteLocation,SiteReport} from '@/lib/schemas';import {defaultContext} from '@/lib/schemas';import {ReportView} from './report-view';import {ReportSkeleton} from './Skeleton';import {AddressSearchBar} from './AddressSearchBar';
const postReport=async([url,payload]:[string,{locationId:number;location:SiteLocation;context:DecisionContext}])=>{const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok)throw new Error(body.error??'Report could not be created');return body as SiteReport};
export function Investigator({initialQuery=''}:{initialQuery?:string}){const [selected,setSelected]=useState<SiteLocation|null>(null),[context,setContext]=useState(defaultContext),[reportKey,setReportKey]=useState<[string,{locationId:number;location:SiteLocation;context:DecisionContext}]|null>(null);const report=useSWR<SiteReport>(reportKey,postReport,{dedupingInterval:300000,revalidateOnFocus:false});if(report.data)return <><button className="sage-button back-button" onClick={()=>setReportKey(null)}>← New investigation</button><ReportView report={report.data}/></>;if(report.isLoading)return <ReportSkeleton/>;return <div className="search-workspace"><div className="search-copy"><span className="section-label">New property screen</span><h2>Know the land<br/>before you commit.</h2><p>Find the property, confirm the location, and SiteSafe will assemble independent bushfire and climate evidence in parallel.</p><div className="search-proof"><span>01 <b>Locate</b></span><span>02 <b>Screen</b></span><span>03 <b>Act</b></span></div></div><div className="panel search-panel"><AddressSearchBar onSelect={setSelected} initialQuery={initialQuery}/>{selected&&<div className="context-form"><div className="selected-location"><span>Selected location</span><strong>{selected.address}</strong><small>{selected.locality} · {selected.lat.toFixed(5)}, {selected.lon.toFixed(5)}</small></div><div className="form-grid"><label>Intended use<select value={context.use} onChange={e=>setContext({...context,use:e.target.value as typeof context.use})}>{['home','renovation','development','farm','warehouse/commercial','general investment'].map(x=><option key={x}>{x}</option>)}</select></label><label>Exposed asset value (AUD)<input type="number" min="0" value={context.assetValue} onChange={e=>setContext({...context,assetValue:+e.target.value})}/></label><label>Holding period (years)<input type="number" min="1" max="100" value={context.holdingYears} onChange={e=>setContext({...context,holdingYears:+e.target.value})}/></label><label>Purchase price, optional (AUD)<input type="number" min="0" placeholder="Only used in comparison" onChange={e=>setContext({...context,purchasePrice:e.target.value?+e.target.value:null})}/></label></div><button className="sage-button full" onClick={()=>setReportKey(['/api/site-report',{locationId:selected.id,location:selected,context}])}>Build climate report <span>→</span></button></div>}{report.error&&<p className="dashboard-alert" role="alert">{report.error.message}</p>}</div></div>}
