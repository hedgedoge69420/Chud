'use client';
import {Area,AreaChart,Bar,BarChart,CartesianGrid,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import {BarChart3} from 'lucide-react';
type Historical={year:number;days_over_35:number};type Projection={indicator:string;minimum:number;median:number;maximum:number};type Adaptation={name:string;low:number;high:number};type ChartData={chartType:'historical_vs_projection'|'adaptation_costs';title:string;historical?:Historical[];projections?:Projection[];data?:Adaptation[];unit:string};
export function GenerativeChartWidget({chart}:{chart:ChartData}){return <article className="copilot-widget chart-widget"><header><BarChart3 size={16}/><span>{chart.title}</span></header>{chart.chartType==='adaptation_costs'?<div className="copilot-chart"><ResponsiveContainer><BarChart data={chart.data??[]} layout="vertical" margin={{left:8,right:8}}><CartesianGrid horizontal={false} stroke="#34483a"/><XAxis type="number" hide/><YAxis dataKey="name" type="category" width={92} tick={{fontSize:9,fill:'#a9b8aa'}}/><Tooltip formatter={v=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(Number(v))}/><Legend/><Bar dataKey="low" name="Low estimate" fill="#8da77e" radius={[0,4,4,0]}/><Bar dataKey="high" name="High estimate" fill="#dce8cd" radius={[0,4,4,0]}/></BarChart></ResponsiveContainer></div>:<><div className="copilot-chart"><ResponsiveContainer><AreaChart data={chart.historical??[]}><defs><linearGradient id="copilotHeat" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#dce8cd" stopOpacity={.5}/><stop offset="100%" stopColor="#dce8cd" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="#34483a"/><XAxis dataKey="year" tick={{fontSize:9,fill:'#a9b8aa'}}/><YAxis tick={{fontSize:9,fill:'#a9b8aa'}}/><Tooltip/><Area dataKey="days_over_35" name="Days >35°C" stroke="#dce8cd" fill="url(#copilotHeat)"/></AreaChart></ResponsiveContainer></div><div className="projection-chips">{chart.projections?.map(p=><span key={p.indicator}><small>{p.indicator.replaceAll('_',' ')}</small><b>{p.minimum} to {p.maximum}</b><em>median {p.median}</em></span>)}</div></>}</article>}
