import {Calculator} from 'lucide-react';
type Scenario={assetValue:number;lowDamageRatio:number;highDamageRatio:number;directDamageLow:number;directDamageHigh:number;interruptionDays:number;interruptionCostPerDay:number;interruptionCost:number;totalLow:number;totalHigh:number;disclaimer:string};const aud=(n:number)=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(n);
export function FinancialCalcCard({scenario}:{scenario:Scenario}){return <article className="copilot-widget calc-widget"><header><Calculator size={16}/><span>Illustrative impact scenario</span></header><strong>{aud(scenario.totalLow)}–{aud(scenario.totalHigh)}</strong><div><span>Direct damage <b>{aud(scenario.directDamageLow)}–{aud(scenario.directDamageHigh)}</b></span><span>Interruption <b>{scenario.interruptionDays} days × {aud(scenario.interruptionCostPerDay)} = {aud(scenario.interruptionCost)}</b></span></div><small>{scenario.disclaimer}</small></article>}
