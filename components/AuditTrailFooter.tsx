import {Clock,Database,ExternalLink} from 'lucide-react';import type {SiteReport} from '@/lib/schemas';
export function AuditTrailFooter({report}:{report:SiteReport}){return <footer className="audit-footer" id="methodology"><div className="audit-heading"><div><span className="section-label">Traceability</span><h2>Sources & audit trail</h2></div><span><Clock size={14}/> Report {report.mode==='demo'?'saved':'generated'} {new Date(report.savedAt).toLocaleString('en-AU')}</span></div><div className="audit-list">{report.sources.map(source=><details key={source.id}><summary><span className={`source-dot ${source.status.status==='success'?'success':'warning'}`}/><Database size={15}/><strong>{source.name}</strong><span>{source.status.status.replace('_',' ')}</span></summary><div><p>{source.status.status==='success'?`Retrieved at ${new Date(source.status.retrievedAt).toLocaleString('en-AU')}`:source.status.message}</p>{source.url&&<a href={source.url} target="_blank" rel="noreferrer">Open source <ExternalLink size={13}/></a>}</div></details>)}</div><p className="audit-disclaimer">SiteSafe is a preliminary screening tool. It is not an engineering assessment, insurance quotation, property valuation, legal opinion or disaster prediction system.</p></footer>}
