'use client';
import {useState} from 'react';import Link from 'next/link';import {usePathname,useRouter} from 'next/navigation';import {ArrowRight,Database,Leaf,Search} from 'lucide-react';
export function HeaderNav(){const [query,setQuery]=useState(''),router=useRouter(),pathname=usePathname();const saved=pathname==='/demo'||pathname==='/compare',live=pathname==='/investigate';function submit(event:React.FormEvent){event.preventDefault();if(query.trim().length>=3)router.push(`/investigate?q=${encodeURIComponent(query.trim())}`)}return <><div className={`live-banner ${saved?'saved':live?'live':'scope'}`}><span/>{saved?<><b>Saved demo data</b><span>No live network calls on this page</span></>:live?<><b>Live data connections active</b><span>Vicmap and Open-Meteo · Preliminary evidence only</span></>:<><b>Transparent climate screening</b><span>Live sources are used only during an investigation</span></>}</div><header className="regrow-nav"><Link className="regrow-brand" href="/"><span><Leaf size={18}/></span>SiteSafe</Link><form className="nav-search" onSubmit={submit}><Search size={15}/><input aria-label="Search Victorian property" value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search a Victorian address"/><button aria-label="Search"><ArrowRight size={15}/></button></form><nav aria-label="Main navigation"><Link href="/investigate">Investigate</Link><Link href="/compare">Compare</Link><Link href="/methodology">Methodology</Link></nav><Link className="sage-button" href="/demo"><Database size={14}/> View demo</Link></header></>}
