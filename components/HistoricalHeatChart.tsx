'use client';
import {Area,AreaChart,CartesianGrid,ReferenceLine,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import type {HeatHistory} from '@/lib/schemas';import {Skeleton} from './Skeleton';
export function HistoricalHeatChart({history}:{history:HeatHistory|null}){if(!history)return <div className="panel"><Skeleton className="skeleton-chart"/><p className="disclosure">Historical data unavailable. No low-exposure inference has been made.</p></div>;const average=history.annual.reduce((s,x)=>s+x.days_over_35,0)/history.annual.length;return <section className="panel chart-panel" aria-labelledby="historical-title"><div className="panel-heading"><div><span className="section-label">Historical exposure</span><h2 id="historical-title">Days above 35°C</h2></div><span className="data-badge">ERA5-Land reanalysis</span></div><div className="chart" role="img" aria-label={`Annual days above 35 degrees Celsius. Average ${average.toFixed(1)} days per year.`}><ResponsiveContainer><AreaChart data={history.annual} margin={{top:16,right:12,left:-18,bottom:0}}><defs><linearGradient id="heatFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#f97316" stopOpacity={.38}/><stop offset="100%" stopColor="#f97316" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="var(--chart-grid)"/><XAxis dataKey="year" axisLine={false} tickLine={false} tick={{fill:'var(--muted)',fontSize:12}}/><YAxis axisLine={false} tickLine={false} tick={{fill:'var(--muted)',fontSize:12}}/><Tooltip contentStyle={{borderRadius:12,border:'1px solid var(--line)',background:'var(--panel)',color:'var(--ink)'}} labelFormatter={year=>`Year ${year}`} formatter={(v)=>[`${Number(v).toFixed(0)} precise days`,'Days >35°C']}/><ReferenceLine y={average} stroke="#64748b" strokeDasharray="5 5" label={{value:`Avg ${average.toFixed(1)}`,position:'insideTopRight',fill:'var(--muted)',fontSize:11}}/><Area type="monotone" dataKey="days_over_35" stroke="#f97316" strokeWidth={3} fill="url(#heatFill)" activeDot={{r:5,strokeWidth:2,fill:'#fff'}}/></AreaChart></ResponsiveContainer></div><p className="disclosure">{history.provenance.period} · {history.retainedYears} complete years · grid-cell reanalysis, not station observations.</p></section>}
