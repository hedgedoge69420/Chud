'use client';import {useState} from 'react';import {demoReports} from '@/lib/demo-data';import {ReportView} from './report-view';
export function DemoClient(){const [id,setId]=useState(demoReports[0].id),report=demoReports.find(item=>item.id===id)!;return <main><div className="demo-switcher"><span><b>Saved demo property</b><small>Switch reports without network calls</small></span><div role="group" aria-label="Choose saved demo property">{demoReports.map(item=><button type="button" aria-pressed={item.id===id} className={'button '+(item.id===id?'':'secondary')} key={item.id} onClick={()=>setId(item.id)}>{item.location.locality}</button>)}</div></div><ReportView report={report} assistant="home"/></main>}
